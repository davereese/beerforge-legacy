import { Component, Input, Output, EventEmitter } from '@angular/core';

// import { Malt } from '../../models-shared/brew.interface';

@Component({
  selector: 'adjunct',
  styleUrls: ['adjunct.component.scss'],
  templateUrl: './adjunct.component.html'
})
export class adjunctComponent {
  // @Input()
  // detail: Malt;
}